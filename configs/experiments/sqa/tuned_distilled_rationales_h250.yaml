# Tuning run: reference + distilled rationales; all 250 held-out questions evaluated with greedy generations.
task: sqa
extends: [../../presets/tuned.yaml, ../../presets/tuned_sqa.yaml]
data: {pipeline: distilled_rationales}
answer: {max_tokens: 384}
train: {eval_every: 400, eval_holdout_n: 250, gen_eval_n: 250, gen_max_new_tokens: 320}
