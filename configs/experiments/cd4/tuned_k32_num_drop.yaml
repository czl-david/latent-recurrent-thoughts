# Tuning run: stage 1 with decoder-side number dropout (the proposer always sees every number).
task: cd4
extends: [../../presets/tuned.yaml, ../../presets/tuned_cd4.yaml]
model: {K_infer: 32}
train: {eval_every: 500, eval_holdout_n: 128, eval_aug_n: 128, gen_eval_n: 64, gen_max_new_tokens: 96}
stage1: {steps: 6000, lr: 1.0e-3, warmup_steps: 200, pipeline: num_drop}
